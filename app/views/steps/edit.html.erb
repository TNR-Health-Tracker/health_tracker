<%= provide(:title, "Edit Walking Entry") %>
<div class="content">
  <h1>Edit Walking Entry</h1>

  <%= render 'form' %>

  <%= link_to 'Show', @step %> |
  <%= link_to 'Back', steps_path %>
  <p class="bottom"></p>

</div>
